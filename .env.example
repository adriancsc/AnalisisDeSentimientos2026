# Variables de Entorno

# Para desarrollo local: copia este archivo como .env y completa los valores

# Credenciales de Firebase (JSON como string, para producción en Render)
# FIREBASE_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# Para desarrollo local, en vez de usar la variable de entorno,
# coloca el archivo firebase-credentials.json en la carpeta backend/
